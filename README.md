**MOVED**: This plugin has been merged into the [orak.nvim](https://github.com/salorak/orak.nvim) ecosystem. It is a collection of simple and small plugins for daily usage. Nothing particularly fancy.

# note.nvim

The simplest way to take notes. Just create a note using `note()` or find a note through `Telescope` with `telescope_note()`.
